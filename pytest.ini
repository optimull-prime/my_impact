[pytest]
markers =
    unit: Unit tests - fast, isolated, use mocks/fixtures
    integration: Integration tests - use real data or controlled temp fixtures
    smoke: Smoke tests - validate shipped demo data works
    slow: Tests that may take longer to run